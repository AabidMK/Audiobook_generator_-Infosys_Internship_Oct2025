# AudioChatBot

## Setup

1. Create virtual environment:
python -m venv venv
venv\Scripts\activate # Windows

2. Install dependencies:


pip install -r requirements.txt


## Run Backend


cd backend
python app.py


## Use Frontend
- Open `frontend/index.html` in a browser.
- Upload a file and download processed output.